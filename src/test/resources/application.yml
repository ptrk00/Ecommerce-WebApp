spring:

  profiles:
    active: test

  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
  flyway:
    enabled: false

  main:
    allow-bean-definition-overriding: true

app:
  imagesPath: src/test/resources/static/images/